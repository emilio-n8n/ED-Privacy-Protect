{
  "name": "ed-privacy-protect",
  "version": "0.0.1",
  "dependencies": {
    "web-ext": "^10.2.0"
  },
  "scripts": {
    "dev": "web-ext run",
    "dev-c": "web-ext run -t chromium",
    "build": "web-ext build",
    "http": "npx http-server"
  }
}
